This folder contains only fragments from library `RxDataSources` because linking whole library is unnecessary if only two files are used.

Files were extracted from version 3.0.2.

P. S. Do you know better way how to do that? I don't want to duplicate code into this library.
